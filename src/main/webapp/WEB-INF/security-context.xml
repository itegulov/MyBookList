<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/security
       http://www.springframework.org/schema/security/spring-security.xsd"
             xmlns:beans="http://www.springframework.org/schema/beans">

    <!--<http pattern="/logout/user/" security="none" />-->

    <http auto-config="false" use-expressions="true">
        <intercept-url pattern="/user/login" access="isAnonymous()"/>
        <intercept-url pattern="/user/sign_up" access="isAnonymous()"/>
        <intercept-url pattern="/profile/settings/**" access="isAuthenticated"/>
        <intercept-url pattern="/profile/**" access="permitAll"/>
        <intercept-url pattern="/user/**" access="permitAll"/>
        <intercept-url pattern="/about" access="permitAll"/>
        <intercept-url pattern="/contacts" access="permitAll"/>
        <intercept-url pattern="/logout" access="permitAll"/>
        <intercept-url pattern="/" access="permitAll"/>
        <intercept-url pattern="/res/**" access="permitAll"/>
        <intercept-url pattern="/books/list" access="hasAnyAuthority('WRITE_PRIVILEGE')"/>
        <intercept-url pattern="/**" access="isAuthenticated()"/>

        <form-login login-page="/user/login"
                             login-processing-url="/login_user"
                             username-parameter="username"
                             password-parameter="password"
                             default-target-url="/"
                             authentication-failure-url="/user/loginfailed"
                             always-use-default-target='true'/>
        <logout invalidate-session="false"  logout-url="/logout" delete-cookies="JSESSIONID" />
        <!--<remember-me remember-me-parameter="remember_me" key="superSecretKey" token-validity-seconds="86400"/>-->
    </http>

    <beans:bean id="userDetailsService" class="ru.mybooklist.service.MyUserDetailsService"/>

    <beans:bean id="logAttempt" class="ru.mybooklist.service.LoginAttemptService"/>

    <authentication-manager>
        <authentication-provider user-service-ref="userDetailsService">
            <password-encoder ref="encoder"/>
        </authentication-provider>
    </authentication-manager>

    <beans:bean id="encoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder">
        <beans:constructor-arg name="strength" value="10"/>
    </beans:bean>
</beans:beans>